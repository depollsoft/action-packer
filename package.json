{
  "name": "action-packer",
  "version": "1.0.0",
  "description": "Self-hostable web UI for managing github actions running locally on-machine. <Experimental personal project -- under development>",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "npm run dev --workspace=backend & npm run dev --workspace=frontend",
    "build": "npm run build --workspace=backend && npm run build --workspace=frontend",
    "test": "npm run test --workspace=backend && npm run test --workspace=frontend",
    "start": "npm run start --workspace=backend"
  },
  "keywords": ["github-actions", "self-hosted", "web-ui"],
  "author": "",
  "license": "ISC"
}
